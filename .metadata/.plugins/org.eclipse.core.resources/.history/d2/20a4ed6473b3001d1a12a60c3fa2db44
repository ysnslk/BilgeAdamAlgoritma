package lesson014;

public class UserManager {

	public void generateAutoEmail(User user) {
		if(user.getName().contains(" ") && user.getSurName().contains(" ")) {
			
		}
		else {
			user.setEmail(user.getName().toLowerCase() + '.' + user.getSurName().toLowerCase() + "@xbanka.com");
		}
	}
}
